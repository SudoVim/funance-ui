import { selectors as account } from "./account";
import { selectors as api } from "./api";
import { selectors as holdings } from "./holdings";

export const selectors = {
  account,
  api,
  holdings,
};
