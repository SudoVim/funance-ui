import { accounts } from "./accounts";
import { selectors as positions } from "./account-positions";
import { selectors as accountPurchases } from "./account-purchases";

export const selectors = {
  accounts,
  positions,
  accountPurchases,
};
